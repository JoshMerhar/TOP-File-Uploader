<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="/stylesheets/styles.css">
        <title>Delete Folder</title>
    </head>
    <body>
        <div class="container">
            <div class="main-content">
                <h1>Are you sure you want to delete "<%= folder.folderName %>" and all of its contents?</h1>
                <h2>THIS ACTION CANNOT BE UNDONE.</h2>
                <form class="input-form" action="/library/folder/<%= folder.id %>/delete" method="POST">
                    <button type="submit" class="submit-button">Delete Folder</button>
                    <a href="/library/folder/<%= folder.id %>">No! Go back</a>
                </form>
            </div>
        </div>
    </body>
</html>